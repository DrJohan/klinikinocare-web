import React from 'react';
import ReactDOM from 'react-dom/client';
import BioWoundApp from './BioWoundApp';
import '../index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BioWoundApp />
  </React.StrictMode>,
);

